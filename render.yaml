services:
  - type: web
    name: rental-bot
    runtime: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python -m bot.main"
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0
